// Usado em desenvolvimento (npm start / ng serve) -- trocado automaticamente
// por environment.prod.ts na hora do build de produção (veja fileReplacements
// em angular.json). Nunca importe este arquivo direto; sempre via api.config.ts.
export const environment = {
  apiBaseUrl: 'http://127.0.0.1:8000'
};
