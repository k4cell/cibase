// Usado no build de produção (ng build), via fileReplacements em
// angular.json -- substitui environment.ts automaticamente, sem precisar
// editar nada na mão a cada deploy.
export const environment = {
  apiBaseUrl: 'https://tixa-base.onrender.com'
};
